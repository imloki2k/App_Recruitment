<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:padding="16dp">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Quản lý ứng tuyển"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/notificationBadge" />

    <TextView
        android:id="@+id/notificationBadge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="12sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/notification_badge_background"
        android:padding="4dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/titleText"
        app:layout_constraintStart_toEndOf="@id/titleText"
        app:layout_constraintEnd_toStartOf="@id/buttonMessage" />

    <ImageButton
        android:id="@+id/buttonMessage"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@android:color/transparent"
        android:src="@android:drawable/ic_menu_send"
        android:contentDescription="Tin nhắn - chuyển sang chọn người chat"
        android:padding="4dp"
        app:layout_constraintTop_toTopOf="@id/titleText"
        app:layout_constraintStart_toEndOf="@id/notificationBadge"
        app:layout_constraintEnd_toStartOf="@id/buttonLogout" />

    <Button
        android:id="@+id/buttonLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Đăng xuất"
        android:textSize="14sp"
        android:backgroundTint="#F44336"
        android:textColor="#FFFFFF"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="@id/titleText"
        app:layout_constraintStart_toEndOf="@id/buttonMessage"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/tv_no_applications"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Không có đơn ứng tuyển nào."
        android:textSize="16sp"
        android:textColor="#757575"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/titleText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:padding="16dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewApplications"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintBottom_toTopOf="@id/buttonCreateInternship"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleText" />

    <Button
        android:id="@+id/buttonCreateInternship"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Tạo thực tập mới"
        android:textSize="16sp"
        android:backgroundTint="#2196F3"
        android:textColor="#FFFFFF"
        android:padding="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="0.9" />
</androidx.constraintlayout.widget.ConstraintLayout>