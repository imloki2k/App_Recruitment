<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IRRProject"
        tools:targetApi="31" >

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- Đăng ký người dùng -->
        <activity android:name=".RegisterActivity" />

        <!-- Danh sách thực tập -->
        <activity android:name=".InternshipListActivity" />

        <!-- Chi tiết thực tập -->
        <activity android:name=".InternshipDetailsActivity" />

        <!-- Lịch sử ứng tuyển -->
        <activity android:name=".ApplicationHistoryActivity" />

        <!-- Lên lịch phỏng vấn -->
        <activity android:name=".InterviewScheduleActivity" />

        <!-- Màn hình bản đồ -->
        <activity android:name=".MapActivity" />

        <!-- Chat giữa sinh viên và nhà tuyển dụng -->
        <activity android:name=".ChatActivity" />

    </application>
</manifest>