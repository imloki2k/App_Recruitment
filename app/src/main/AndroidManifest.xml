<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IRRProject"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MyApplicationsActivity"
            android:exported="true"
            android:label="Đơn Ứng Tuyển Của Tôi"/>

        <!-- Đăng ký người dùng -->
        <activity android:name=".RegisterActivity" />

        <!-- Danh sách thực tập -->
        <activity android:name=".InternshipListingsActivity" />

        <!-- Chi tiết thực tập -->
        <activity android:name=".InternshipDetailsActivity" />

        <!-- Tạo thực tập (cho Recruiter) -->
        <activity android:name=".CreateInternshipActivity" />

        <!-- Bảng điều khiển Recruiter -->
        <activity android:name=".RecruiterDashboardActivity" />

        <!-- Chỉnh sửa thực tập -->
        <activity android:name=".EditInternshipActivity" />

        <activity android:name=".ProfileActivity" />


    </application>
</manifest>